@if (chipDTO is null)
{
	<Loading />
}
else
{
	<MudDialog>
		<DialogContent>
			<ChipForm @ref="chipForm" chipDTO="chipDTO" OnValidSubmit="EditAsync" ReturnAction="Return" />
		</DialogContent>
	</MudDialog>
}