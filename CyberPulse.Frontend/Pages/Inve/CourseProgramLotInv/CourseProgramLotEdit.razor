@if (courseProgramLotDTO is null)
{
	<Loading />
}
else
{
	<MudDialog>
		<DialogContent>
			<CourseProgramLotForm @ref="courseProgramLotForm" CourseProgramLotDTO="courseProgramLotDTO" OnValidSubmit="EditAsync" ReturnAction="Return" />
		</DialogContent>
	</MudDialog>
}